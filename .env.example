# Server
HOST=0.0.0.0
PORT=3001

# Database
DB=./data/smxp.db

# Admin API secret — set a strong random value before running in production
# Generate one with: openssl rand -hex 32
ADMIN_SECRET=your_secret_here
